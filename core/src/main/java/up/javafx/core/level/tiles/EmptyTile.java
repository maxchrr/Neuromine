package up.javafx.core.level.tiles;

import up.javafx.core.level.Grid;

/**
 * A neutral tile with no specific threat or number.
 */
public class EmptyTile extends Tile {

	public EmptyTile(Grid grid, int x, int y) {
		super(grid, x, y);
	}

}
