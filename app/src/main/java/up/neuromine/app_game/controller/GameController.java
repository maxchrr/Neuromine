package up.neuromine.app_game.controller;

import up.mvc.*;

/*Logique commune (score, tours) */
public class GameController extends Controller{

    public GameController(Model model, View view){
        super(model,view);
    }

    @Override
    public void handleAction(Action action) {
        switch(action){
            case:
                break;
            default:
        }
    }

    @Override
    protected void handleSignal(Signal signal) {
        switch(signal){
            case:
                break;
            default:
        }
    }

}
